<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Landmarks</title>
  </head>
  <body>
    <h1>Landmarks</h1>
  	<% @landmarks.each do |l| %>
  	<p><a href="/landmarks/<%=l.id%>"><%= "#{l.name} (#{l.year_completed})" %></a></p>
  	<% end %>
  </body>
</html>
